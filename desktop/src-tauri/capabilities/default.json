{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window: core APIs, clipboard read/write, system notifications (SSH approval alerts while unfocused), and native file dialogs (attachment pick/save).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "clipboard-manager:allow-write-text",
    "clipboard-manager:allow-read-text",
    "notification:default",
    "dialog:default"
  ]
}
